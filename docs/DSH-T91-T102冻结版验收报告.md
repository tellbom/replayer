# DSH T-91~T-102 冻结版验收报告

日期：2026-08-25
被测版本：**d25b8fe**（fix(T-94): remove business-specific diff mappings）
执行者：GLM（测试角色——只记录、复现、定位，不修产品代码）
测试对象：模拟完整 OA regression fixture（mock-portal :4000 + mock-legacy-sys :4100）+ 真实 OA（localhost:5173，账号 196045）
探针脚本：tmp/ta*.mjs、tmp/ro*.mjs（全部可复跑）

---

## P0 / P1 单列

**本次未发现 P0。**（重复副作用红线 7 轮零重复、语义漂移被拦、身份锁代码+e2e 佐证、未确认写不自动重发——四条红线全部守住。）

| # | 级别 | 问题 | 归类 |
|---|---|---|---|
| **F-1** | **P1** | **preflight 的独立 fetch GET 与后续 navigate 不是同一表单实例**：preflight GET 生成 pendingForm#1 → s2 navigate 重新 GET 生成 #2（服务端焚毁式校验下 #1 作废）→ s7 POST 带 #1 值 → **400**。「GET 即轮换 + 单活值」形态（__VIEWSTATE/seqCode 家族）在「preflight + UI navigate」组合下**没有可用配置路径**（network 步 extract 在 navigate 步不执行） | Data-flow / Replay |
| **F-2** | **P1** | generateDraft 对含 null 值动作崩溃（ZodError `params Expected string, received null`）：报销场景（费用明细行为 null label 的 click 动作）draft 生成直接失败，record.partial.json 保留但无 draft | DOM / Analyzer |
| **F-3** | **P1（未修复已知项，非回归）** | 审批人搜索链的 response-value-match 在真实 OA 未触发：leave2 场景 `search 张三→POST approverId=EMP001` 值链存在，correlation 仍全 time-window，approverId 标 TODO_UNRESOLVED（不乱绑——方向安全，但 T-96 溯源能力未覆盖此形态） | Data-flow |
| F-4 | P2 | draft 的 `ui.value` 绑参错位：ext（分机）fill 步绑到「故障描述」参数（analyze 把两个 fill 并到一个参数）；且 fill 动作带 `expectsRedirect` 时 `waitForNavigation` 必超时（fill 不导航） | Replay / Analyzer |
| F-5 | P2 | enumMap 只含录制值——跨参数需人工补全（设计局限，报错清晰可操作） | Analyzer（设计） |
| F-6 | P3 | daemon 死后 state 文件残留 active（pid 校验会拒绝复用，但文件不清理）；门户失效等登录时无 DSH 前台横幅（靠登录页本身） | Browser（体验） |

---

## 1. 执行摘要

冻结版 d25b8fe 在模拟完整 OA fixture 上：**基线达标**（build✓ / unit 180/180 / e2e 114+3+0——历史 entries/skills 隔离后）、**主链跑通**（门户→票据→子系统→record→analyze→回放→服务端核验，302 提交不崩且恰好 +1）、**四红线全守**。T-91~T-102 的修复在真实链路逐项见效：T-95 recorder 导航竞态修复、T-102 expectsRedirect+postcondition 收敛、T-97 controlSemantics、T-98 首跑监督、T-100 attach-first 常驻、T-99/T-92 认证探测（302/HTML 形态不再抛「认证状态未知」）、T-94 preflight 动态值、semantic-drift 硬停。**遗留三个 P1**：焚毁式单活值的 preflight/navigate 时序（F-1）、analyzer null 崩溃（F-2）、审批人值链溯源（F-3）。真实 OA 六场景测了 4 个（purchase/leave2 完整、expense 录制成功但 draft 崩=F-2 实证、travel 级联受阻为探针侧问题）。

**裁决：CONDITIONAL GO**——模拟 OA regression fixture 在 d25b8fe 上主链与安全红线验证通过；F-1/F-2/F-3 修复前，「焚毁式动态值表单」「含 null 动作的录制」两类场景不可用。

---

## 2. 测试前基线

| 项 | 结果 | 预期 | 判定 |
|---|---|---|---|
| workspace build | PASS | PASS | ✅ |
| unit | **180/180** | 180/180 | ✅ |
| e2e（首轮） | 112 passed / 3 skipped / **2 failed** | 113/3/0 | ⚠ 环境 |
| e2e（隔离历史遗留后） | **114 passed / 3 skipped / 0 failed** | 113/3/0 | ✅（多 1 个=新测试，正向） |

首轮 2 failed 的根因（均为**工作树污染**，非产品回归）：
1. `A9` — `entries/` 含历史会话遗留的 `realoa.yaml` 等旧格式文件（无 `requiresAuth`），被 `--entries ./entries` 全量加载。隔离后通过。
2. `T-92 非 JSON 响应` — 4-worker 并发下偶发（单独跑通过，全量重跑通过）。

按文档三节要求执行了「先查 Entry 污染」：隔离目录 `tmp/legacy-entries-quarantine/`、`tmp/legacy-skills-quarantine/`。

---

## 3. 完整模拟 OA 主链（第四节）

**PASS（录制侧）/ FAIL（回放侧，F-1）**

| 环节 | 结果 | 证据 |
|---|---|---|
| Portal → 模拟人工认证 → portal session | ✅ | 探针点「模拟指纹认证」按钮（仅初始化） |
| 一次性票据消费 | ✅ | `/sso?token=` 一次性，重放被 302 回门户（反向验证） |
| Legacy subsystem session | ✅ | JSESSIONID 建立 + persistent 化回归技巧（e2e fixture 同款） |
| record（含 302 提交） | ✅ **不崩** | T-95 生效：actions=7 network=2，POST document 302 被捕获 |
| C19 排除 | ✅ | `?token=`/`/sso` 零泄漏 |
| analyze | ✅ | preflight=`__VIEWSTATE,__TOKEN,seqCode`（T-94）、expectsRedirect=true、postcondition 自动推断、controlSemantics×5（T-97） |
| 人工检查 TODO | ✅ | desc/ext=TODO_UNRESOLVED（不猜测，正确方向） |
| replay | ❌ **F-1** | s7 POST 400：preflight fetch GET 的值被 s2 navigate 轮换作废 |
| 服务端核验 | ✅ 执行了 | 录制侧 records 1→2 恰好+1；回放侧增量 0（正确地没有乱写） |

**F-1 最小复现**（ta2h）：
```
同会话 GET#1(preflight fetch) seqCode=SEQ-MT87O0Y0
GET#2(navigate)     seqCode=SEQ-MT87O0YC   ← 两次不同
POST(旧#1值) → 400
```
期望：preflight 提取与提交所用表单实例同源。实际：产品当前无「navigate 后页面内提取」的步骤形态（navigate 步的 network.extract 不执行，preflight 是全局前置独立 fetch）。
问题归类：**Data-flow / Replay**（通用性：任何 GET-轮换+单活值形态，不限于 ASP.NET）。
怀疑产品通用性问题：**是**（fixture/config 无解）。

---

## 4. Cookie/Session 矩阵（第五节）

| 组合 | 首次进入 | 同身份恢复 | 子失效静默重建 | 门户失效 | identity |
|---|---|---|---|---|---|
| A portal=session sub=session | ✅ | ✅ | ✅ 3.2s 无感 | 落登录页等待→用户登录后恢复 ✅ | 稳定 ✅ |
| B portal=persistent sub=session | ✅ | ✅ | ✅ | 同上 ✅ | 稳定 ✅ |
| C portal=session sub=persistent | ✅ | ✅ | ✅ | 同上 ✅ | 稳定 ✅ |
| D portal=persistent sub=persistent | ✅ | ✅ | ✅ | 同上 ✅ | 稳定 ✅ |

- **静默重建**：expire-sub 后 ensureEntry 3.2s 内经门户自动重建，无需人工（ta3b 实测，A/D 两模式）
- **门户失效**：ensureEntry 落在登录页等待，模拟用户登录后正确恢复 resolved(authState=authenticated)（T-99 生效——上一轮的「认证状态未知」异常**已消失**）
- **identity 变化阻断**：fixture 单用户无法端到端构造。佐证：① reentry.ts:51-55 digest 不等即抛 IdentityChangedError（代码证据）；② 基线 e2e `T-77: 换身份登录立即中止` PASS（identityChanged:true 断言通过）。判定：**PASS（佐证）/ NOT VERIFIED（端到端）**
- sessionProbe 与 identityProbe 同 URL：**能力正常**（ta-main 同端点 /api/whoami 全链路工作）

---

## 5. 剩余会话时长（第六节）

| 验证 | 结果 |
|---|---|
| 配置驱动（无硬编码 4h/30min） | ✅ code 审查：session-daemon.ts:128-141 纯 expectedPortalTtlMs/warnBeforeExpiryMs；unit 覆盖（session.test 6/6） |
| remaining > warnBefore → 不提醒 | ✅ unit + e2e 端到端：临期 entry（TTL 20s/warn 15s）启动后 t+35s 横幅才出现 |
| remaining <= warnBefore → 提醒 | ✅ 横幅文案：「DSH：会话配置 TTL 预计时长已耗尽；实际状态仍以实时探测为准」 |
| session 实际失效但 TTL 未到 → probe 判失效 | ✅ 手动 expire-sub 后 daemon 探测周期触发**静默重建**回 active（probe 为真源 + 门户生命线双验证） |

## 6. Browser 常驻（第七节）

| 验证 | 结果 |
|---|---|
| `dsh session start` + 一次人工登录 | ✅ daemon 直接 active（CDP endpoint 暴露） |
| record × 3 | ✅ 全部成功（actions=3×3），浏览器不关 |
| replay × 3 | ✅ ok=true×3，daemon 持续 active |
| 已存在页面注入完整 | ✅ `{locator:"object", pwgen:"function"}` |
| 后续新页面注入完整 | ✅ 同上 |
| kill daemon → 再 record | ✅ 死 state 被拒绝复用（resolveSessionEndpoint→undefined），record 走正常 launch 路径成功 |
| 不误关其它浏览器 | ✅ 17 个 chrome 进程（含真实 OA）全程存活 |
| 残留 state 清理 | ⚠ F-6：state 文件残留（不复用，但不清文件） |

---

## 7. Legacy 表单（第八节）

| 项 | 结果 |
|---|---|
| native select 录制 | ✅（T-94 后 `select` 动作入 record：`{type:'select', label:'报销类型', value:'办公报销'}`） |
| radio / checkbox | ✅ 录制且带 controlSemantics（T-97：`{tagName:input, type:radio, name:urgency, value:URGENT, checked:true}`） |
| checkbox checked=false / initialFormState / 默认值 | ✅ draft 生成 `checked:` 字段与 initialFormState；**默认值未点击而进入写请求**的场景：seqCode/__VIEWSTATE 全部进 preflight（能溯源），desc/ext 标 TODO_UNRESOLVED（无法溯源不猜）——符合第八节要求 |
| 「不静默当常量」 | ✅ 未点击的 radio 默认值在 draft 中以 controlSemantics 呈现待人工确认；hidden 值走 preflight 依赖 |

## 8. 语义漂移（第九节）

**PASS（三项控件全部）**

```
录制：radio nth=1 = 紧急(URGENT)
注入漂移（前方插「行政通道」radio）→ nth=1 = 一般(NORMAL)
回放（supervisedVerification）：
THROWN: SemanticDriftError: 步骤 s4：录制时此处为「urgency = URGENT [radio]」，
当前为「urgency = NORMAL [radio]」。该位置型定位已发生语义漂移；
为避免错误业务操作，执行已停止，请重新录制。
服务端增量：0
```

- **radio**：✅ 副作用前停止（kind=semantic-drift 语义，e2e V97-3 断言 `rerecordReason.kind='semantic-drift'` 基线通过）
- **checkbox / native select**：✅ e2e V97-5/V97-6（checkbox and native select drift are blocked）基线通过
- 对照上一轮 P0（静默点错 radio 且 ok=true）：**T-98 supervised + controlSemantics 联合修复实证有效**

## 9. response-value-match（第十节）

**FAIL（F-3，非回归——上一轮已报告的未修复项）**

真实 OA leave2 场景值链（P→Q→R→W 齐备）：
```
用户值 P: 审批人「张三」
request Q: GET /api/employees/search?keyword=张三
response R: {"data":[{"id":"EMP001","name":"张三",...}]}
mutating W: POST /api/applications/leave body.approverId="EMP001"
```
实际 `_correlation`：全部 `time-window`（3 处），`response-value-match` 0 处；draft 中 `approverId: TODO_UNRESOLVED`。
期望：W 中 approverId 可追溯 R（method=response-value-match, confidence=high）。
**不强行 high 的纪律**：✅ 保守标 UNRESOLVED（未错绑）。
归类：Data-flow（analyzer 的值链识别未覆盖「搜索响应数组元素 → 写请求 body 字段」）。
补充：purchase 场景（search→待 POST）因表单校验拦截未达提交，无新数据。

## 10. 302 / Navigation 提交（第十一节）

| 项 | 结果 |
|---|---|
| recorder 不崩溃 | ✅（TA-1：302 提交完整录制） |
| partial 已提前落盘 | ✅（e2e `an abnormal recorder exit preserves an atomic incomplete snapshot` 基线通过；F-2 场景 record.partial.json 保留） |
| navigation-after-submit 被记录 | ✅（pages 含 /records） |
| expectsRedirect 正确 | ✅（draft 自动置 true） |
| opaque response 不直接判失败 | ✅（`redirect response is opaque; resolving by postcondition` → outcome_unknown → 等 postcondition） |
| postcondition 判最终结果 | ✅ 成功路径：录制侧 records+1 与 postcondition 一致 |
| outcome_unknown→postcondition 命中→confirmed_success | ✅（navigate 链正常时） |
| outcome_unknown→postcondition 未命中→confirmed_failure/中止 | ✅（F-1 场景实测：s7 判 `confirmed_failure: redirecting write requires postcondition verification`，**没有假装成功**）——对照上一轮「ok=true 但零写入」的 P0，本版**已修复** |

## 11. 重复副作用（第十二节）

**PASS**：TA-2 七轮回放（原参/跨参/5 连发），每轮服务端增量**恰好 1 或 0（失败轮）**，无任何一轮出现 >1。302/opaque/postcondition-未命中/session 重建各形态下**无自动重发**（reentries=0 全程）。

## 12. 一次性动态表单值（第十三节）

- ✅ 无字段名特判（代码审查：preflight 按配置 selector 提取，`input[name="__VIEWSTATE"]` 是 entry/skill 配置不是产品逻辑）
- ❌ **F-1**：「再次执行重新提取新值」在 preflight 层面成立（每轮 preflight 重新 GET 提取），但与 navigate 的**实例不同源**（见第 3 节）
- ✅ 旧值不复用：K-9 反证（同 VIEWSTATE 二次 POST → 400）

## 13. 浏览器关闭重开（第十四节）

| 模式 | cookie 存在 | sessionProbe | 需人工登录 | subsystem 恢复 | digest 一致 |
|---|---|---|---|---|---|
| portal persistent | ✅ | ✅ | 否 | ✅ 经门户静默重建 | ✅ |
| portal session | ❌（正确失效） | ❌ | **是**（落登录页等待，无静默失败/超时崩溃） | 登录后 ✅ | ✅（同身份） |

（C.6 双模式实测；浏览器 Cookie 语义未被人为改变）

---

## 14. 真实 OA 多场景（5173，196045）

| 场景 | 录制 | draft | 服务端 | 结论 |
|---|---|---|---|---|
| 采购申请 | ✅ 11 actions / 5 net | ✅ 13 步 H=11 L=0 unresolved=0 | 提交被「成本中心」前端校验拦（探针漏选） | **完整 PASS（录制/分析链）** |
| 请假（复测） | ✅ 10 actions / 3 net（含 POST leave→200） | ✅ 9 步 H=9 L=0，3 unresolved（approverId 等） | POST 200 落库 | **PASS**（F-3 的 approverId UNRESOLVED 在此实证） |
| 费用报销 | ✅ 15 actions | ❌ **generateDraft 崩溃**（F-2：null 值动作） | — | 录制 PASS / 分析 **FAIL** |
| 出差申请 | ✅ 9 actions（提交被行程校验拦） | ❌ UnusedParameterError（radio 的动态 name `el-id-xxx` 被参数化后未引用） | — | 部分（探针无法完成 Element Plus 三级级联+teleport 的真实用户路径） |
| 合同用印 | — | — | — | **ENVIRONMENT BLOCKED（探针侧）**：多轮 UI 状态残留导致定位超时，成本超限 |
| 会议室预订 | — | — | — | 同上（筛选型页面，非提交表单） |

真实 OA 专项观察（给 Codex）：
1. **Keycloak check-sso 深竞态**：每次新 launch 浏览器触发 5173→KC(auth)→5173(`#/login&state=`)→3p-cookies→#/login→#/dashboard 闭环约 7s。record 冷启动（launch 模式）若 onReady 在闭环完成前 goto 会被打断卡死；**daemon 常驻（attach）模式无此问题**（一次闭环后稳定）——T-100 的 attach-first 恰好是正确答案，建议文档化「bearer+SSO 跳转型系统优先用 session 常驻」。
2. **Element Plus 新式 el-select（wrapper 形态）与 cascader（teleport 多面板）**：录制器能否稳定回放此类组件未在本轮验证到提交级（探针交互成本过高），建议列入下轮 fixture。
3. analyzer 对 **radio 动态 name（el-id-xxx）** 的参数化产生 UnusedParameterError（travel 场景）——与 F-2 同族（分析器对动态标识的鲁棒性）。

---

## 15. 结论四分汇总

| 项 | 判定 |
|---|---|
| 基线三件套 | **PASS** |
| 主链（录制侧） | **PASS** |
| 主链（回放侧，焚毁式动态值） | **FAIL**（F-1） |
| Cookie/Session 矩阵 ×4 | **PASS** |
| identityLock | **PASS（佐证）/ NOT VERIFIED（端到端）** |
| TTL advisory | **PASS** |
| Browser 常驻（record×3/replay×3/kill-daemon） | **PASS** |
| Legacy 表单（第八节全项） | **PASS** |
| 语义漂移（radio/checkbox/select） | **PASS** |
| response-value-match | **FAIL**（F-3，非回归） |
| 302/Navigation 提交 | **PASS** |
| 重复副作用红线 | **PASS** |
| 一次性动态值（preflight 同源性） | **FAIL**（F-1） |
| 浏览器关闭重开 ×2 | **PASS** |
| 真实 OA 场景 | purchase/leave2 **PASS**、expense **FAIL**（F-2）、travel **部分**、seal/meeting **ENVIRONMENT BLOCKED（探针侧）** |

## 16. 失败项十要素（按文档十五节）

**F-1**（焚毁式单活值 preflight 时序）
1. 复现：`node tmp/ta1-main-chain.mjs` + `node tmp/ta2-replay-verify.mjs`（s7 400）；最小化 `node tmp/ta2h-verify-theory.mjs`
2. 输入：skill 含 preflight(GET form)+s2 navigate+s7 POST(引用 preflight 值)；服务端 GET 即轮换单活值
3. 实际：POST 400（VIEWSTATE/seqCode 与会话当前 pendingForm 不匹配）
4. 期望：POST 提取与提交同源，成功落库
5. 副作用：无（400 被拒，服务端增量 0——**安全失败**）
6. 服务端数据：records 无新增（正确）
7. commit：d25b8fe
8. 归类：**Data-flow / Replay**
9. 怀疑产品通用性：**是**（「GET-轮换+单活值+UI导航」组合无配置解）
10. fixture/config 问题：否（K-5/K-9 是真实形态）

**F-2**（analyzer null 崩溃）
1. 复现：`node tmp/ro2f-scenarios-v2.mjs expense`
2. 输入：报销表单录制（费用明细行为 null label 的 click 动作，record.partial.json actions=5+）
3. 实际：`ZodError: params Expected string, received null`，draft 生成中断
4. 期望：跳过 null 或标 TODO，draft 正常产出
5. 副作用：无
6. 服务端：未达提交
7. commit：d25b8fe
8. 归类：**DOM / Analyzer**
9. 通用性：是（任何含 null label 动作的录制）
10. fixture：否

**F-3**（审批人值链未识别）
1. 复现：`node tmp/ro2f-scenarios-v2.mjs leave2` 后看 draft.yaml 的 `_correlation` 与 `approverId`
2. 输入：search 张三（响应 EMP001）→ POST leave（body approverId=EMP001）
3. 实际：correlation=time-window×3，approverId=TODO_UNRESOLVED
4. 期望：response-value-match/high，approverId 可溯源
5. 副作用：无（回放需人工补参）
6. 服务端：POST 200 落库正确（人工修正后）
7. commit：d25b8fe
8. 归类：**Data-flow**
9. 通用性：是（上一轮 B2 报告已给出实现判据：name+department→id 三元组、取最后匹配响应、按字段值不按位置）
10. fixture：否

## 17. 未能完成的项

| 项 | 原因 |
|---|---|
| identityLock 端到端（换身份） | fixture 单用户，无法构造（白盒 mock 成本超限）；代码+e2e 佐证 |
| 真实 OA 合同用印/会议室场景 | 探针 UI 适配成本超限（多面板级联/筛选页形态），**非产品判定** |
| 出差场景提交级回放 | 同上（Element Plus cascader teleport 交互，探针无法完成真实用户路径；DSH 对此类组件的回放能力建议下轮专项） |
| F-1 的正解配置验证 | 产品当前无「navigate 后页面内提取」步骤形态——配置空间内无解，只能记缺陷 |

## 18. 最终裁决

# CONDITIONAL GO

**本裁决仅表示**：模拟完整 OA regression fixture（mock-portal + mock-legacy-sys + 真实 OA 部分场景）在 **d25b8fe** 上——主链录制侧、四条 P0 红线（无静默错单/无重复写入/身份锁/漂移拦截）、Cookie/Session 矩阵、TTL、常驻、302 收敛——**验证通过**。

**条件（进内网前必须）**：
1. F-1：焚毁式单活值表单的「提取-提交同源」路径（建议：navigate 步执行 network.extract，或 preflight 支持「页面态提取」模式）
2. F-2：analyzer 对 null/动态标识动作的鲁棒性（skip+TODO 而非崩溃）
3. F-3：审批人搜索值链的 response-value-match（判据已在 B2 报告）

**不得扩大解读**：本结论不等于「真实 OA 全部验证通过」或「DSH 支持所有 OA」——真实 OA 仅覆盖 4/6 场景且其中出差/用印/会议室的组件复杂形态（cascader/teleport/新式 select）未验证到提交级。

---

**文档结束**
